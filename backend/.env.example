DATABASE_URL=sqlite:///./data/faces.db
MODEL_DIR=models
SECRET_KEY=your-secret-key-change-in-production
INITIAL_INVITE_CODE=  # Optional: code for first admin registration (no spaces)
RESET_INVITE_CODE=  # Optional: code for admin password reset (no spaces)
